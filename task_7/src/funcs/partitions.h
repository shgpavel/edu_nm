#ifndef PARTITIONS_H
#define PARTITIONS_H

#include "func.h"

inline void linspace(size_t num_of_points) {
  for (size_t i = 0; i < num_of_points; ++i) {
    double point = A + (double)i * (B - A) / (double) (num_of_points - 1);
    
  }
}

#endif
