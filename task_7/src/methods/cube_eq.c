/* SPDX-License-Identifier: Apache-2.0 */

#include <jemalloc/jemalloc.h>
#include "../include/vector.h"

vector *cube_eq_roots(vector *polynom) {
	vector *roots = (vector *)malloc(sizeof(vector));
	vector_init(roots, 3, sizeof(double));
	
	
	
	return 0.0;
}
