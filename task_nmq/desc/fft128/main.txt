PROBLEM

FFT 2D 128x128
Таймаут: 600 c, прогонов: 1, режим: function

Условие
FFT 2D 128x128
Нужно реализовать прямое двумерное преобразование Фурье для матрицы размера `128 × 128`.

Формат входных данных
На вход подаются `128 * 128` строк.
В каждой строке записана пара чисел: `re im`.
Это действительная и мнимая части одного элемента входной матрицы.
Формат выходных данных
Выведите `128 * 128` строк.
В каждой строке должна быть пара целых чисел: `re im`.
Обёртка округляет результат прямого FFT до целых и сравнивает его с ожидаемым ответом.
Функция участника
typedef struct {
    float re;
    float im;
} complex_float;

void contest_fft_2d(complex_float *matrix, const float *twiddle_cos, const float *twiddle_sin);
Как вход и выход связаны с функцией
`matrix` указывает на массив из `128 * 128` элементов `complex_float`
элементы матрицы лежат подряд в памяти в row-major
`twiddle_cos` и `twiddle_sin` уже подготовлены обёрткой
функция должна записать результат прямого 2D FFT обратно в `matrix`
после возврата из функции обёртка округляет `matrix[i].re` и `matrix[i].im` и печатает результат в `stdout`
свой `main()` писать не нужно
Открытых тестов: 2, скрытых тестов: 2

Таймаут: 600 c, прогонов: 1, режим: function

Архив для локального запуска на QEMU: скачать bundle

Поддерживаются `.c`, `.cpp`, `.s`, `.S`, `.asm`. Посылка может состоять из одного или двух файлов. В этом режиме нельзя присылать свой `main`: раннер сам линкует ваше решение со своей обёрткой и вызывает функцию или набор функций, описанных в условии задачи. Обёртка сама подготавливает вход, делает замер через `clock()` на кристалле и сравнивает результат.
